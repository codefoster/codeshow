(function () {
	"use strict";

	WinJS.Namespace.define("Controls", {
		WelcomeControl: WinJS.UI.Pages.define("/pages/hub/welcomeControl.html", {
			ready: function (element, options) {
				options = options || {};

				//Should we add something dynamically or just write what we want in the html page?
			}
		})
	});
})();